export interface I_user {
    username: string;
    balance: number;
    password: string;
}

export interface Product {
    id: number;
    name: string;
    price: number;
    category: string;
    image: string;
}
